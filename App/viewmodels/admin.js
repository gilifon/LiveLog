define(['viewmodels/shell'], function (shell) {

    var vm = {
        activate: function () {
            
        },
        compositionComplete: function () {
            
        },
        shell: shell
    };

    return vm;
});